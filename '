#version 460

#extension GL_GOOGLE_include_directive : enable

layout(local_size_x = 256) in;

layout(push_constant) uniform PC {
    uint num_objs;
};

struct ObjectBounds {
    vec4 sphere_bounds;
    vec4 extents;
};

VK2_DECLARE_STORAGE_BUFFERS_RO(ObjectBoundsBuffer){
ObjectBounds bounds[];
} object_bounds[];
struct DrawCmd {
    uint index_cnt;
    uint instance_cnt;
    uint first_index;
    int vertex_offset;
    uint first_instance;
};

void main() {
    if (gl_GlobalInvocationID.x > num_objs) {
        return;
    }
    // get the object. test its frustum against the view frustum
}
